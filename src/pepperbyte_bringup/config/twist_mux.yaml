twist_mux:
  ros__parameters:
    topics:
      navigation:
        topic: nav_vel
        timeout: 0.5
        priority: 10
      joystick:
        topic: joy_vel
        timeout: 0.5
        priority: 20
    # Joystick (priority 20) overrides Nav2 (priority 10)
